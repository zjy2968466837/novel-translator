# v1.1.1

## Fix Release

### 修复内容

- 修复 DeepSeek Beta 前缀续写模式下的跨章节上下文注入：前文参考现在可在章节之间连续传递，提升称谓与语气一致性。
- 优化上下文尾部提取，过滤章节尾部的非正文元信息（如评价/反馈引导语），减少对后续章节翻译的干扰。
- 修复章节识别：跳过 `nav/toc/cover/titlepage/copyright` 等非正文文档，避免被当作可翻译章节。
- 提升日志输出兼容性：在部分终端编码环境下避免日志字符导致的输出异常。

### 依赖与版本

- 统一版本号为 `1.1.1`。
- 保持运行依赖与打包配置一致（含 `requests`）。
